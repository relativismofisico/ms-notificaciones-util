# ── Selector de ambiente ───────────────────────────────────────────────────────
# Uso: AMBIENTE_PIPE=qa ./scripts/run-tests.sh smoke
# Valores válidos: dev | qa | prod
# El script run-tests carga el archivo config/${AMBIENTE_PIPE}.properties
ambiente=dev